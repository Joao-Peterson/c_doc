Important: 

- Make a better tutorial on syntax, for every data type, reinforce the cast to (size_t) for strings and bindata

Features :

- Implement static interfaces with structs, dunno, something like this:

#define REGISTER_INTERFACE(struct) (#struct)

REGISTER_INTERFACE(
    typedef struct{
        uint8_t data;
        int other_data;
        char *a_string;
    }my_interface;
)

